name: Setup Bun dependencies
description: Install Bun and restore node_modules from bun.lock cache (caller must checkout first)

inputs:
  bun-version:
    description: Bun version (`BUN_VERSION` repository variable when unset)
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Setup Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: ${{ inputs.bun-version != '' && inputs.bun-version || '1.3.14' }}

    - name: Cache node_modules
      id: cache-deps
      uses: actions/cache@v5
      with:
        path: node_modules
        key: node-modules-${{ runner.os }}-${{ hashFiles('bun.lock') }}
        restore-keys: |
          node-modules-${{ runner.os }}-

    - name: Install dependencies
      if: steps.cache-deps.outputs.cache-hit != 'true'
      shell: bash
      run: bun install --frozen-lockfile
