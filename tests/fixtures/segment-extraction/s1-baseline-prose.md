---
title: Baseline
description: Short learn page with inline code and one fenced block.
---

# Hello React

Use the `useState` hook to store local state in a component.

```js
function Counter() {
  const [count, setCount] = useState(0);
  return count;
}
```

State updates trigger a re-render.
